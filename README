Install
-------

To install a workload or parser apply the yaml file with `kubectl apply -f <file>`.
To install all, do `find . -name '*.yaml' -exec kubectl apply -f {} \;`

Creating a new workload
-----------------------

To create a new workload, create a new directory in the `workloads` directory.

Output files should be written to /output/.
